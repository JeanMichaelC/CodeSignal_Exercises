(() => {
	// My solution
	function solution(n: number): number {
		return Number('9'.repeat(n));
	}

	// Great solutions of others
	function nissim_l(n: number): number {
    return Math.pow(10, n) - 1
}
})

